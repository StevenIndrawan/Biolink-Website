@model BioLinkWeb.Models.ProfileSettingViewModel

@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Settings - @Model.DisplayName</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />
    <style>
        body {
            margin: 0;
            padding: 40px;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #6a11cb, #2575fc, #ff6a00, #ee0979);
            background-size: 400% 400%;
            animation: gradientMove 20s ease infinite;
        }
        @@keyframes gradientMove {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .settings-container {
            background: rgba(255,255,255,0.1);
            backdrop-filter: blur(15px);
            border-radius: 20px;
            padding: 30px;
            max-width: 650px;
            width: 100%;
            color: white;
            box-shadow: 0 8px 30px rgba(0,0,0,0.25);
            animation: fadeIn 0.8s ease;
        }

        @@keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        h2 {
            font-size: 24px;
            font-weight: bold;
            text-align: center;
            margin-bottom: 25px;
        }

        .profile-pic {
            text-align: center;
            margin-bottom: 25px;
        }

        .profile-pic img {
            width: 120px; height: 120px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid rgba(255,255,255,0.5);
            box-shadow: 0 6px 15px rgba(0,0,0,0.4);
        }

        .form-group {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 15px;
        }

        .form-group label {
            flex: 0 0 140px; /* lebar label */
            font-size: 14px;
            font-weight: 600;
            color: #eee;
            margin: 0;
            text-align: right;
        }

        .form-group input,
        .form-group textarea,
        .form-group select {
            flex: 1;
            padding: 10px 14px;
            border-radius: 10px;
            border: none;
            outline: none;
            font-size: 14px;
            background: rgba(255,255,255,0.15);
            color: #fff;
            transition: 0.25s;
        }

        .form-group input:focus,
        .form-group textarea:focus,
        .form-group select:focus {
            background: rgba(255,255,255,0.25);
            box-shadow: 0 0 0 2px rgba(255,255,255,0.4);
        }

        textarea { resize: none; }

        .radio-group {
            display: flex;
            gap: 20px;
            align-items: center;
        }

        .btn {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 12px;
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            margin-top: 10px;
            transition: all 0.3s ease;
        }

        .btn-save {
            background: linear-gradient(90deg, #4cafef, #2196f3);
            color: white;
            box-shadow: 0 4px 14px rgba(0,0,0,0.3);
        }
        .btn-save:hover { transform: translateY(-2px); }

        .btn-delete {
            background: #e74c3c;
            color: white;
        }
        .btn-delete:hover { background: #c0392b; }

        .accordion {
            margin-top: 20px;
        }
        .accordion-header {
            padding: 12px;
            border-radius: 10px;
            background: rgba(255,255,255,0.15);
            display: flex;
            justify-content: space-between;
            cursor: pointer;
            margin-bottom: 6px;
            transition: 0.3s;
        }
        .accordion-header:hover {
            background: rgba(255,255,255,0.25);
        }
        .accordion-content {
            max-height: 0;
            overflow: hidden;
            transition: all 0.3s ease;
            background: rgba(255,255,255,0.08);
            border-radius: 10px;
            padding: 0 12px;
        }
        .accordion-content.active {
            max-height: 500px;
            padding: 12px;
            margin-bottom: 10px;
        }
        
        .form-group select option {
            color: #222 !important;        /* semua opsi teks gelap */
            background: #fff !important;   /* latar putih */
        }

        .back-btn {
            display:block;
            text-align:center;
            margin-top:20px;
            color:#ddd;
            font-size:14px;
            text-decoration:none;
        }
        .back-btn:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <div class="settings-container">
        <div class="profile-pic">
            <img id="profilePreview" src="@Model.ProfileImageUrl" alt="@Model.DisplayName" />
        </div>

        <h2>⚙️ Edit Profile</h2>

        <!-- FORM UPDATE PROFILE -->
        <form asp-action="Settings" method="post" enctype="multipart/form-data">
            @Html.AntiForgeryToken()

            <div class="form-group">
                <label>Foto Profil</label>
                <input type="file" name="profileImage" accept="image/*" onchange="previewImage(event)" />
            </div>

            <div class="form-group">
                <label>Username</label>
                <input asp-for="Username" readonly />
            </div>

            <div class="form-group">
                <label>Display Name</label>
                <input asp-for="DisplayName" />
            </div>

            <div class="form-group">
                <label>Bio</label>
                <textarea asp-for="Bio" rows="2"></textarea>
            </div>

            <div class="form-group">
                <label>Background Theme</label>
                <select asp-for="Background" onchange="previewBackground(this.value)">
                    <option value="linear-gradient(135deg,#6a11cb,#2575fc)">Ungu - Biru</option>
                    <option value="linear-gradient(135deg,#ff6a00,#ee0979)">Orange - Pink</option>
                    <option value="linear-gradient(135deg,#36d1dc,#5b86e5)">Cyan - Biru</option>
                    <option value="linear-gradient(135deg,#ff9a9e,#fad0c4)">Pink - Putih</option>
                    <option value="linear-gradient(135deg,#00c6ff,#0072ff)">Langit - Biru Tua</option>
                    <option value="linear-gradient(135deg,#f7971e,#ffd200)">Kuning - Emas</option>
                </select>
            </div>

            <div class="form-group">
                <label>Profil Visibility</label>
                <div class="radio-group">
                    <label>
                        <input type="radio" name="IsPublic" value="true" @(Model.IsPublic ? "checked" : "") /> Public
                    </label>
                    <label>
                        <input type="radio" name="IsPublic" value="false" @(!Model.IsPublic ? "checked" : "") /> Private
                    </label>
                </div>
            </div>

            <button type="submit" class="btn btn-save">💾 Simpan Perubahan</button>
        </form>

        <!-- LINKS SECTION -->
        <h3 style="margin-top:25px;">🔗 Links</h3>
        <div id="links-container">
            @for (int i = 0; i < Model.Links.Count; i++)
            {
                <div class="accordion">
                    <div class="accordion-header" onclick="toggleAccordion(@i)">
                        <span>@Model.Links[i].Title</span>
                        <i class="bi bi-chevron-down"></i>
                    </div>
                    <div id="accordion-@i" class="accordion-content">
                        <form asp-action="DeleteLink" method="post" style="text-align:right;">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="id" value="@Model.Links[i].Id" />
                            <button type="submit" class="btn btn-delete">🗑 Hapus</button>
                        </form>

                        <div class="form-group">
                            <label>Title</label>
                            <input value="@Model.Links[i].Title" readonly />
                        </div>
                        <div class="form-group">
                            <label>URL</label>
                            <input value="@Model.Links[i].Url" readonly />
                        </div>
                        <div class="form-group">
                            <label>Icon</label>
                            <input value="@Model.Links[i].Icon" readonly />
                        </div>
                    </div>
                </div>
            }
        </div>

        <!-- ADD NEW LINK -->
        <h3 style="margin-top:25px;">➕ Tambah Link Baru</h3>
        <form asp-action="AddLink" method="post">
            @Html.AntiForgeryToken()
            <div class="form-group">
                <label>Title</label>
                <input name="Title" required />
            </div>
            <div class="form-group">
                <label>URL</label>
                <input name="Url" required />
            </div>
            <div class="form-group">
                <label>Icon</label>
                <select name="Icon" class="icon-select">
                    <option value="bi bi-link-45deg">🔗 Default</option>
                    <option value="bi bi-facebook">📘 Facebook</option>
                    <option value="bi bi-instagram">📸 Instagram</option>
                    <option value="bi bi-twitter-x">🐦 Twitter/X</option>
                    <option value="bi bi-youtube">▶️ YouTube</option>
                    <option value="bi bi-tiktok">🎵 TikTok</option>
                    <option value="bi bi-whatsapp">💬 WhatsApp</option>
                    <option value="bi bi-telegram">✈️ Telegram</option>
                    <option value="bi bi-github">💻 GitHub</option>
                    <option value="bi bi-linkedin">💼 LinkedIn</option>
                </select>
            </div>
            <button type="submit" class="btn btn-save">Tambah Link</button>
        </form>

        <a asp-controller="Biolink" asp-action="Index" asp-route-username="@Model.Username" class="back-btn">
            ⬅️ Kembali ke Profile
        </a>
    </div>

    <script>
        function toggleAccordion(index) {
            const content = document.getElementById("accordion-" + index);
            content.classList.toggle("active");
        }

        function previewImage(event) {
            const reader = new FileReader();
            reader.onload = function () {
                document.getElementById('profilePreview').src = reader.result;
            };
            reader.readAsDataURL(event.target.files[0]);
        }

        function previewBackground(gradient) {
            document.body.style.background = gradient;
            document.body.style.backgroundSize = "400% 400%";
            document.body.style.animation = "gradientMove 20s ease infinite";
        }

        // Saat halaman load, set background sesuai database
        window.onload = function () {
            var current = "@Model.Background";
            if (current) previewBackground(current);
        }
    </script>
</body>
</html>
